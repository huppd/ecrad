FC = pgf90
CPPFLAGS = -Mpreprocess

ifndef DEBUG
OPTFLAGS = -O3 -acc -ta=nvidia,cc70 -Mcuda
else
OPTFLAGS = -O0
endif

WARNFLAGS = -Minform=inform
BASICFLAGS = -module ../mod -Mbyteswapio
DEBUGFLAGS = -g
OMPFLAG = -mp=nonuma
